<td class="start-time">
  {{raw-date event.start_time}}
</td>

<td class="name">
  {{#if showTopics}}
    {{#each event.topic_ids as |topic_id|}}
      <a
        {{on "click" (fn this.openTopic topic_id)}}
        target="_blank"
        rel="noopener noreferrer"
        role="button"
      >{{event.name}}</a>
    {{/each}}
  {{else}}
    {{event.name}}
  {{/if}}
</td>

<td class="tags">
  <div class="event-tags">
    {{#if providerLabel}}
      <EventTag
        @label={{providerLabel}}
        @href="/admin/plugins/events/provider"
        @class="provider"
      />
    {{/if}}
    {{#if event.series_id}}
      <EventTag
        @label={{i18n "admin.events.event.series.label"}}
        @class="series"
      />
    {{/if}}
  </div>
</td>

<td class="select-event">
  <Input @type="checkbox" @checked={{event.selected}} />
</td>